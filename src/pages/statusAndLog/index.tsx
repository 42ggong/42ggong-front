import React from "react";
import * as S from "./style";
import { useNavigate } from "react-router-dom";

const DisCard = () => {
  const navigate = useNavigate();
  return <S.Div>상태와 기록</S.Div>;
};

export default DisCard;
