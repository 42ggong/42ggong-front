import React from "react";
import * as S from "./style";
import { useNavigate } from "react-router-dom";

const DisCard = () => {
  const navigate = useNavigate();
  return <S.Div>보관하기</S.Div>;
};

export default DisCard;
